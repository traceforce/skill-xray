# Error strings reproduce the retired Python scanner's exception messages verbatim, which the
# golden fixtures still expect, so ST1005's lower-case rule does not apply.
checks = ["inherit", "-ST1005"]
