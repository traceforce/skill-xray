# Error strings reproduce the Python oracle's exception messages verbatim (the parity harness
# compares them), so ST1005's lower-case rule does not apply to this port.
checks = ["inherit", "-ST1005"]
